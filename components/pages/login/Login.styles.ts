import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    display: "flex",
    width: "100%",
    height: "100%",

    justifyContent: "center",
    alignItems: "center",
  },
  form: {
    marginTop: 64,
    marginBottom: 40,
  },
  bigTitle: {
    marginTop: 80,
  },
  inputContainer: {
    marginBottom: 24,
  },
  inputLabel: {
    marginBottom: 8,
  },
  input: {
    width: 160,
  },
});
