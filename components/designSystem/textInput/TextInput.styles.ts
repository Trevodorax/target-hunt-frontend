import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    width: 120,
    height: 40,
    borderColor: '#000',
    borderWidth: 1,
    padding: 10,
    borderRadius: 4,
    textAlign: 'center'
  }
})