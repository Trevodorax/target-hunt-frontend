import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  roomCodeContainer: {
    width: "100%",
    justifyContent: "center",
    alignItems: "center",
    marginTop: 64,
  },
  title: {
    fontSize: 24,
    fontWeight: "500",
    paddingBottom: 24,
  },
  selector: {
    display: "flex",
    flexDirection: "row",
    gap: 8,
  },
});
